### Added by Zinit's installer
if [[ ! -f $HOME/.local/share/zinit/zinit.git/zinit.zsh ]]; then
    print -P "%F{33} %F{220}Installing %F{33}ZDHARMA-CONTINUUM%F{220} Initiative Plugin Manager (%F{33}zdharma-continuum/zinit%F{220})…%f"
    command mkdir -p "$HOME/.local/share/zinit" && command chmod g-rwX "$HOME/.local/share/zinit"
    command git clone --depth=1 https://github.com/zdharma-continuum/zinit "$HOME/.local/share/zinit/zinit.git" && \
        print -P "%F{33} %F{34}Installation successful.%f%b" || \
        print -P "%F{160} The clone has failed.%f%b"
fi

source "$HOME/.local/share/zinit/zinit.git/zinit.zsh"
autoload -Uz _zinit
(( ${+_comps} )) && _comps[zinit]=_zinit

# Load a few important annexes, without Turbo
# (this is currently required for annexes)
zinit light-mode for \
    zdharma-continuum/zinit-annex-as-monitor \
    zdharma-continuum/zinit-annex-bin-gem-node \
    zdharma-continuum/zinit-annex-patch-dl \
    zdharma-continuum/zinit-annex-rust

### End of Zinit's installer chunk

zinit lucid light-mode for \
    pick'shell.plugin.zsh' xiao0yy/shell

zinit as"null" lucid light-mode for \
    pick'empty.zsh' atload'eval "$(starship init zsh)"' xiao0yy/shell

zinit as"null" wait lucid light-mode for \
    pick'empty.zsh' atload"alias cat='bat --style plain,header,header-filesize,grid'
                           alias l='eza -lbghH --icons=always'
                           alias ll='eza -lbghH --icons=always'
                           alias la='eza -labghH --icons=always'
                           alias ld='eza -lDbghH --icons=always'
                           alias lad='eza -laDbghH --icons=always'
                           alias http='xh'
                           alias https='xh'" xiao0yy/shell \
    pick'empty.zsh' atload'export ATUIN_NOBIND="true"
                           eval "$(atuin init zsh)"
                           bindkey "^r" _atuin_search_widget' xiao0yy/shell \
    pick'empty.zsh' atload'export _ZO_ECHO=1
                           eval "$(zoxide init zsh --no-cmd)"
                           alias cdi="__zoxide_zi"' xiao0yy/shell

zinit lucid light-mode for \
    atinit"zicompinit; zicdreplay" \
        zdharma-continuum/fast-syntax-highlighting \
    atload"_zsh_autosuggest_start" \
        zsh-users/zsh-autosuggestions \
    blockf atpull'zinit creinstall -q .' \
        zsh-users/zsh-completions

zinit wait lucid light-mode for \
    wfxr/forgit \
    Aloxaf/fzf-tab

setopt HIST_IGNORE_DUPS
